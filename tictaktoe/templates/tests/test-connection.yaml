apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tictaktoe.fullname" . }}-test-connection"
  labels:
    {{- include "tictaktoe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tictaktoe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
